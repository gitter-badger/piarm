/*
 |--------------------------------------------------------------------------
 | Handles GPIO pin changes
 |--------------------------------------------------------------------------
**/

class GpioHandler {

    handlePinChange(channel, value) {

    }
}
let run = new GpioHandler();
export default run;