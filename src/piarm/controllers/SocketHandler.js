/*
 |--------------------------------------------------------------------------
 | Handle socket events
 |--------------------------------------------------------------------------
**/

class Handler {

    handleCommand(command) {

    }
}
let run = new Handler();
export default run;