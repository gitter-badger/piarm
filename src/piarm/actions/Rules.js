/*
 |--------------------------------------------------------------------------
 | Actions to interact with the Rules store
 |--------------------------------------------------------------------------
**/

import { Actions } from 'flummox'

export default
class Rules extends Actions {

    addRule(rule) {

        return rule;
    }

    updateRule(rule) {

        return rule;
    }

    updateStore() {

        return true;
    }
}